# Required for VPP/DPDK SCHED_FIFO busy-poll workers on isolcpus.
# Without this, the kernel RT throttle (~50ms+/s) stalls workers and Digi
# x520lan idle/sparse ICMP+ARP replies flush on a ~1s boundary (100–800ms).
kernel.sched_rt_runtime_us = -1
